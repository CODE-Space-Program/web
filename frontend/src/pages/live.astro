---
import Layout from "../layouts/Layout.astro";
import background from "../assets/rocket.jpg";
import { FlightControls } from "../components/FlightControls";

if (!Astro.cookies.has("auth")) {
  return Astro.redirect("/");
}

// export const prerender = false;

// const isLoggedIn = Astro.cookies.has("token");
---

<Layout>
  <img src="/logo.svg" alt="Astro logo" width="200" />
  <img src={background.src} alt="Rocket" fetchpriority="high" id="background" />

  <!-- <div style="width: 100vw; height: 40vh">
    {
      isLoggedIn ? (
        <Charts client:visible />
      ) : (
        <p>
          Please log in to see the chart.{" "}
          <a href="/login" aria-label="Login page">
            Login
          </a>
        </p>
      )
    }
  </div> -->

  <FlightControls client:visible />
</Layout>

<style>
  #background {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -1;
  }

  .inner-left-bottom {
    max-width: 420px;

    width: 50%;

    position: absolute;

    top: 50%;
    left: 50px;
  }

  h1 {
    max-width: 90%;
    font-size: 48px;
    line-height: 48px;

    margin: 0;

    text-transform: uppercase;
  }
  p {
    margin-top: 20px;
    margin-bottom: 15px;

    line-height: 1.6;

    font-size: 1rem;
  }
</style>
